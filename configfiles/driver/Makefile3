# SPDX-License-Identifier: GPL-2.0-only

include $(TOPDIR)/rules.mk

BOARD:=rockchip
BOARDNAME:=Rockchip
FEATURES:=ext4 audio usb usbgadget display gpio fpu pci pcie rootfs-part boot-part squashfs
SUBTARGETS:=armv8

KERNEL_PATCHVER:=6.6

define Target/Description
	Build firmware image for Rockchip SoC devices.
endef

include $(INCLUDE_DIR)/target.mk

DEFAULT_PACKAGES += uboot-envtools partx-utils e2fsprogs mkf2fs kmod-gpio-button-hotplug ethtool

KERNELNAME:=Image dtbs

define Kernel/Patch/YT921X
	grep -q "obj-y += tag_yt921x.o" $(LINUX_DIR)/net/dsa/Makefile || \
	echo 'obj-y += tag_yt921x.o' >> $(LINUX_DIR)/net/dsa/Makefile
endef

KERNEL_PATCH_HOOKS += YT921X

$(eval $(call BuildTarget))
